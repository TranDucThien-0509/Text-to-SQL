# ── Thông tin chung ──
experiment_name: "test_spider_qwen_no_db"
base_dir: "D:/Uni/DS319/Project/word-level"
db_dir: "D:/Uni/DS319/Project/word-level/database"
output_dir: "result"

# ── Tắt các tính năng yêu cầu Database thật ──
use_execution_guided: false
use_self_repair: false
use_cell_value: false

# ── Cấu hình LLM ──
llm_model: "qwen/qwen3-8b"
llm_temperature: 0.0

# ── Model ────────────────────────────────────────────────────────────────
embedding_model: "BAAI/bge-m3"

# ── Chạy thử nghiệm nhỏ ──
test_limit: 10